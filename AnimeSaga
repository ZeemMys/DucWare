local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "DucWare — Anime Saga",
    Icon = 0,
    LoadingTitle = "Wait a minute, the script is loading",
    LoadingSubtitle = "by DucWare",
    Theme = "Amethyst",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "DucWare",
        FileName = "DucWareZ"
    },
    Discord = {
        Enabled = true,
        Invite = "PnHj485p",
        RememberJoins = false
    },
    KeySystem = true,
    KeySettings = {
        Title = "Get Key",
        Subtitle = "Key System",
        Note = "Key: DucWare",
        FileName = "Key",
        SaveKey = true,
        Key = {"DucWare"}
    }
})

Rayfield:Notify({
    Title = "Enjoy",
    Content = "Have Fun :)",
    Duration = 5,
    Image = "number"
})

-- Tạo tab Lobby
local LobbyTab = Window:CreateTab("Lobby", 4483362458)

-- Hàm gửi mã và kiểm tra phản hồi
local function redeemCode(code)
    local success, response = pcall(function()
        game:GetService("ReplicatedStorage").Event.Codes:FireServer(code)
    end)

    if success then
        -- Lắng nghe phản hồi từ server
        game:GetService("ReplicatedStorage").Event.Codes.OnClientEvent:Connect(function(response)
            -- Lắng nghe phản hồi nhưng không thông báo quá nhiều
            if response == "Thành công" then
                print("✅ Mã " .. code .. " đã redeem thành công!")
            else
                print("❌ Lỗi khi redeem mã " .. code)
            end
        end)
    else
        print("❌ Lỗi khi gửi mã " .. code)
    end
end

-- Button để redeem tất cả mã code
LobbyTab:CreateButton({
    Name = "Redeem All Codes",
    Callback = function()
        -- Disable button to avoid repeated clicks
        LobbyTab:DisableButton("Redeem All Codes")

        -- Gửi từng mã và kiểm tra phản hồi
        local codesToRedeem = {"Release", "SorryForDelay", "SorryForShutdown"}
        local redeemSuccess = true

        for _, code in ipairs(codesToRedeem) do
            redeemCode(code)
            wait(2) -- Đợi 2 giây giữa các lần gửi mã để tránh spam server
        end

        -- Thông báo chỉ một lần khi hoàn tất
        if redeemSuccess then
            Rayfield:Notify({
                Title = "🎉 Hoàn tất",
                Content = "Đã thử redeem tất cả mã!",
                Duration = 5
            })
        else
            Rayfield:Notify({
                Title = "❌ Lỗi",
                Content = "Đã gặp lỗi khi redeem một số mã.",
                Duration = 5
            })
        end

        -- Re-enable the button after all codes have been processed
        wait(5) -- Wait before re-enabling the button to avoid spam
        LobbyTab:EnableButton("Redeem All Codes")
    end
})
