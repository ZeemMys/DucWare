local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()  -- Load Rayfield Library

local Window = Rayfield:CreateWindow({
    Name = "DucWare - Anime Saga",
    Icon = 0, -- Icon in Topbar. Set to 0 for no icon.
    LoadingTitle = "Loading Script...",
    LoadingSubtitle = "by DucWare",
    Theme = "Amethyst", -- Theme choice for Rayfield
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "DucWare",
        FileName = "config"
    }
})

-- Tạo một Tab trong cửa sổ
local LobbyTab = Window:CreateTab("Lobby", 4483362458)

-- Tạo một Button trong Tab Lobby cho mã code 'Release'
LobbyTab:CreateButton({
    Name = "Redeem Code 'Release'",  -- Tên nút bấm
    Callback = function()
        -- Lấy sự kiện ReplicatedStorage để gửi mã code
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code "Release" tới server
        local success, err = pcall(function()
            CodesEvent:FireServer("Release")
        end)

        if success then
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Mã code 'Release' đã được nhập thành công.",
                Duration = 3
            })
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "Lỗi khi gửi mã code: " .. err,
                Duration = 3
            })
        end
    end
})

-- Tạo thêm một button cho mã code 'SorryForDelay'
LobbyTab:CreateButton({
    Name = "Redeem Code 'SorryForDelay'",  -- Tên nút bấm
    Callback = function()
        -- Lấy sự kiện ReplicatedStorage để gửi mã code
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code "SorryForDelay" tới server
        local success, err = pcall(function()
            CodesEvent:FireServer("SorryForDelay")
        end)

        if success then
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Mã code 'SorryForDelay' đã được nhập thành công.",
                Duration = 3
            })
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "Lỗi khi gửi mã code: " .. err,
                Duration = 3
            })
        end
    end
})

-- Tạo thêm một button cho mã code 'SorryForShutdown'
LobbyTab:CreateButton({
    Name = "Redeem Code 'SorryForShutdown'",  -- Tên nút bấm
    Callback = function()
        -- Lấy sự kiện ReplicatedStorage để gửi mã code
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code "SorryForShutdown" tới server
        local success, err = pcall(function()
            CodesEvent:FireServer("SorryForShutdown")
        end)

        if success then
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Mã code 'SorryForShutdown' đã được nhập thành công.",
                Duration = 3
            })
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "Lỗi khi gửi mã code: " .. err,
                Duration = 3
            })
        end
    end
})
