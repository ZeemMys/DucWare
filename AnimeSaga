local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "DucWare — Anime Saga",
    Icon = 0, -- Icon in Topbar
    LoadingTitle = "Wait a minute, the script is loading",
    LoadingSubtitle = "by DucWare",
    Theme = "Amethyst",
    ConfigurationSaving = {
       Enabled = true,
       FolderName = "DucWare",
       FileName = "DucWareZ"
    },
 
    Discord = {
       Enabled = true,
       Invite = "PnHj485p",
       RememberJoins = false
    },
 
    KeySystem = true,
    KeySettings = {
       Title = "Get Key",
       Subtitle = "Key System",
       Note = "Key: DucWare",
       FileName = "Key",
       SaveKey = true,
       Key = {"DucWare"}
    }
 })

Rayfield:Notify({
   Title = "Enjoy",
   Content = "Have Fun:)",
   Duration = 5,
   Image = "number",
})

local LobbyTab = Window:CreateTab("Lobby", 4483362458)

-- Button to redeem specific code
LobbyTab:CreateButton({
    Name = "Redeem Release Code",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code Release
        local success, err = pcall(function()
            CodesEvent:FireServer("Release")
        end)

        if success then
            print("[✔] Đã gửi code: Release")
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Đã gửi code: Release",
                Duration = 3
            })
        else
            warn("[✘] Lỗi khi gửi code: Release", err)
            Rayfield:Notify({
                Title = "Lỗi",
                Content = "Không thể gửi code: Release",
                Duration = 3
            })
        end
    end,
})

-- Button to redeem "SorryForDelay"
LobbyTab:CreateButton({
    Name = "Redeem SorryForDelay Code",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code SorryForDelay
        local success, err = pcall(function()
            CodesEvent:FireServer("SorryForDelay")
        end)

        if success then
            print("[✔] Đã gửi code: SorryForDelay")
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Đã gửi code: SorryForDelay",
                Duration = 3
            })
        else
            warn("[✘] Lỗi khi gửi code: SorryForDelay", err)
            Rayfield:Notify({
                Title = "Lỗi",
                Content = "Không thể gửi code: SorryForDelay",
                Duration = 3
            })
        end
    end,
})

-- Button to redeem "SorryForShutdown"
LobbyTab:CreateButton({
    Name = "Redeem SorryForShutdown Code",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

        -- Gửi mã code SorryForShutdown
        local success, err = pcall(function()
            CodesEvent:FireServer("SorryForShutdown")
        end)

        if success then
            print("[✔] Đã gửi code: SorryForShutdown")
            Rayfield:Notify({
                Title = "Code Redeemed",
                Content = "Đã gửi code: SorryForShutdown",
                Duration = 3
            })
        else
            warn("[✘] Lỗi khi gửi code: SorryForShutdown", err)
            Rayfield:Notify({
                Title = "Lỗi",
                Content = "Không thể gửi code: SorryForShutdown",
                Duration = 3
            })
        end
    end,
})
