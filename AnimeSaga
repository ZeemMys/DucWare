local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CodesEvent = ReplicatedStorage:WaitForChild("Event"):WaitForChild("Codes")

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "DucWare — Anime Saga",
    Icon = 0,
    LoadingTitle = "Wait a minute, the script is loading",
    LoadingSubtitle = "by DucWare",
    Theme = "Amethyst",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "DucWare",
        FileName = "DucWareZ"
    },
    Discord = {
        Enabled = true,
        Invite = "PnHj485p",
        RememberJoins = false
    },
    KeySystem = true,
    KeySettings = {
        Title = "Get Key",
        Subtitle = "Key System",
        Note = "Key: DucWare",
        FileName = "Key",
        SaveKey = true,
        Key = {"DucWare"}
    }
})

Rayfield:Notify({
    Title = "Enjoy",
    Content = "Have Fun :)",
    Duration = 5,
    Image = "number"
})

local LobbyTab = Window:CreateTab("Lobby", 4483362458)

-- Danh sách mã
local codeList = {"Release", "SorryForDelay", "SorryForShutdown"}

-- Hàm redeem chạy giống tay người dùng
local function RedeemLikeHuman(code)
    -- chuẩn hóa
    code = tostring(code):gsub("^%s*(.-)%s*$", "%1")
    print("💥 Đang redeem:", code)
    CodesEvent:FireServer(code)
end

-- Nút redeem
LobbyTab:CreateButton({
    Name = "🔥 Redeem All Codes",
    Callback = function()
        -- chạy độc lập khỏi UI context
        spawn(function()
            for _, code in ipairs(codeList) do
                RedeemLikeHuman(code)
                Rayfield:Notify({
                    Title = "Đã gửi mã",
                    Content = code,
                    Duration = 2
                })
                wait(1.5)
            end

            Rayfield:Notify({
                Title = "✅ Hoàn tất",
                Content = "Tất cả mã đã được gửi.",
                Duration = 5
            })
        end)
    end
})
